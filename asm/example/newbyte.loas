my_global: dv #0

; constants: [2, 3]
; locals: [x, y]
; globals: [my_global]
main:
    ; inx ----- | stack ------

    mov x, #2
    ; pushc 0   | 2
    ; pushl 0   | 2, x
    ; store     |
    ; x = 2

    mov y, x
    ; pushl 0   | x
    ; load      | 2
    ; pushl 1   | 2, y
    ; store     |
    ; y = x = 2

    mov my_global, #3
    ; pushl 0   | x
    ; load      | 2
    ; pushl 1   | 2, y
    ; store     |
    ; y = x = 2

    ret
